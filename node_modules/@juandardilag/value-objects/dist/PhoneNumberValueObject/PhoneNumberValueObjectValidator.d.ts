import { IValidatable } from "../ValueObject";
export declare class PhoneNumberValueObjectValidator implements IValidatable<string> {
    private _options?;
    constructor(_options?: {
        required: boolean;
    } | undefined);
    validate(value: string): false | void | Error;
}
