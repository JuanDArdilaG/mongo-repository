import { IValidatable } from "../ValueObject/IValidatable";
export declare class EnumValueObjectValidator<T extends Object> implements IValidatable<T> {
    private _values;
    constructor(_values: T[]);
    validate(value: T): Error | false | void;
}
